<!DOCTYPE html>
<html>
<head>
    <title>Login | The App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&display=swap" rel="stylesheet">
</head>
<body class="d-flex justify-content-center align-items-center vh-100 text-white">
    <div class="d-flex align-items-center gap-5">
        <div>
            <div class="bg-white text-dark p-4 rounded" style="width: 400px;">
                <h2 style="font-family: 'Orbitron', sans-serif;" class="text-primary">THE APP</h2>
                <p class="text-muted">Start your journey</p>
                <h5>Sign in to The App</h5>

        {% if error is defined and error %}
            <div class="alert alert-danger">
                {{ error.messageKey|trans(error.messageData, 'security') }}
            </div>
        {% endif %}


            <form method="post">
                <input type="email" name="_username" placeholder="Email" class="form-control mb-2" required>
                <input type="password" name="_password" placeholder="Password" class="form-control mb-2" required>
                
                <div class="form-check mb-3">
                    <input class="form-check-input" type="checkbox" name="_remember_me" id="rememberMe">
                    <label class="form-check-label" for="rememberMe">
                        <b>Remember me</b>
                    </label>
                </div>
                
                <button type="submit" class="btn btn-primary w-100">Sign in</button>
            </form>
            </div>

            <div class="d-flex justify-content-between mt-4 style="width: 800px;">
                <div>
                    <span class="text-muted">Don't have an account?</span>
                    <a href="/register" class="text-decoration-none ms-1">Sign up</a>
                </div>
                <div>
                    <a href="/forgot-password" class="text-decoration-none">Forgot password?</a>
                </div>
            </div>
        </div>

        <div>
        <img src="{{ asset('images/login-icon.png') }}" alt="Logo" class="img-fluid">
        </div>
    </div>
</body>
</html>

